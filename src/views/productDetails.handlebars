<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{product.name}} Details</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #e8eff5; /* Fondo principal */
            color: #495057; /* Texto principal */
            font-family: Arial, sans-serif;
        }

        .container {
            margin-top: 4rem;
        }
        h1 {
            color: #4794e6; /* Azul primario */
            text-align: center;
        }
        .product-details {
            color: grey;
            padding: 20px;
            border-radius: 20px;
            box-shadow: 0 4px 8px rgba(104, 63, 1, 0.253);
            background-color: #fae9e3; /* Fondo de la tarjeta de detalles */
            margin: 1rem;
            height: 100%;
            width: 100%;
        }

        .product-details h2 {
            color: #4794e6; /* Azul primario */
            font-size: 1.25rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 2rem;
        }

        .product-details img {
            width: 100%;
            height: 300px;
            border-radius: 15px 15px 0 0;
        }

        .card-title {
            font-weight: 700;
            text-decoration: none;
            color: #4794e6; 
        }
        
        .product-info {
          font-size: 1.5rem;
            color: grey;
        }

        .quantity-form {
            display: flex;
            align-items: center;
        }

        .quantity-input {
            margin-left: 3%;
            width: 3rem;
            border-radius: 10px;
        }

        .add-to-cart-btn, .details-btn {
            margin-top: 1rem;
            border-radius: 12px;
        }

        .add-to-cart-btn {
            background-color: #3cb371; /* Verde de éxito */
            color: #fff; /* Texto blanco */
        }

        .add-to-cart-btn:hover {
            background-color: #3cb371; /* Cambié el color en hover al mismo que el de fondo */
        }

        .details-btn {
            background-color: #eee0b9; /* Amarillo de advertencia */
            color: #495057; /* Texto secundario */
        }
    </style>
</head>

<body>

    <div class="container">
        <div class="product-details">
            <h1><a href="/api/products/{{product._id}}" class="card-title display-3">{{product.title}}</a></h1>
            <div class="row">
                <div class="col-md-4">
                    <img src="{{product.thumbnail}}" alt="{{product.name}}">
                </div>
                <div class="col-md-8">
                    <h2></h2>
                    <div class="product-info">
                        <p class="card-text">{{product.description}}</p>
                        <p class="card-text">${{product.price}}</p>
                        <form action="/api/cart/{{user.cartId}}/products/{{product._id}}" method="POST">
                            <input type="hidden" name="products[0][item]" value="{{product._id}}" />
                            <div class="quantity-form">
                                <p class="card-text">Quantity:</p>
                                <input type="number" name="quantity" value="1" min="1" max="100"
                                    class="form-control quantity-input">
                            </div>
                            <button type="submit" class="btn add-to-cart-btn">Add to Cart</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
