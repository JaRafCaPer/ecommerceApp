<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #e8eff5;
            color: darkgrey;
            font-family: Arial, sans-serif;
        }

        .container {
            margin-top: 4rem;
        }

        .profile-header {
            background-color: #4794e6; /* Blue primary color */
            color: #fff;
            padding: 20px;
            text-align: center;
            border-radius: 15px;
            margin-bottom: 20px;
        }

        .profile-header h1 {
            font-size: 2.5rem;
            font-weight: bold;
        }

        .user-info {
            background-color: #ebeae5;
            border-radius: 15px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            padding: 20px;
            margin-bottom: 20px;
        }

        .user-info h2 {
            font-weight: bold;
            color: #4794e6;
        }

        .btn-primary {
            background-color: #4794e6;
            color: #fff;
            border-radius: 12px;
            width: 20%;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }
        .btn-user{
            display: flex;
            justify-content: space-around;
        }
    </style>
</head>

<body>
    <div class="containerPrincipal">
        <div class="profile-header">
            <h1>User Profile</h1>
        </div>
        <div class="btn-user text-center">
            <a href="/api/products/listproducts" class="btn btn-primary">Manage Your Products</a>
            <a href="/api/session/ticket/user" class="btn btn-primary">Tickets</a>
            <a href="/api/session/logout" class="btn btn-primary">Logout</a>
        </div>
        <div class="user-info">
            <h2>First Name:</h2>
            <p>{{first_name}}</p>
            <h2>Last Name:</h2>
            <p>{{last_name}}</p>
            <h2>Age:</h2>
            <p>{{age}}</p>
            <h2>Email:</h2>
            <p>{{email}}</p>
            <h2>Role:</h2>
            <p>{{rol}}</p>
          
            
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
